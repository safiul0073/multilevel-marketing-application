import {
    loginWithPassword,
    EmailSender,
    OTPSender,
  } from './auth';

  export const LoginWithPasswordFunc = loginWithPassword;
  export const EmailSenderFunc = EmailSender;
  export const OTPSenderFunc = OTPSender;
